// ⚠️ Barrel seguro: solo config, constantes y datos.
// fetchUsersList se importa por ruta directa (usa prisma → server-only).
export { USER_CONFIG } from './config/user.constants'
export { userColumns } from './config/user.columns'
